# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '10.0'
use_frameworks!

target 'pokeCloneGame' do
  # Comment the next line if you're not using Swift and don't want to
  pod 'Alamofire', '~> 4.3'

  # Pods for pokeCloneGame

  target 'pokeCloneGameTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'pokeCloneGameUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
